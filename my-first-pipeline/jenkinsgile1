pipeline {
    agent {
        docker { image 'node:latest' }
    }
    stages {
        stage('test') {
            steps {
                sh 'node --version'
            }
        }
    }
    stages {
 agent {
        docker { image 'mysql:latest' }
    }
steps {
                sh 'mysql --version'
            }
}
